\documentclass[aps,preprint,onecolumn,superscriptaddress,nofootinbib]{revtex4-2}

\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{colorlinks=true,citecolor=blue,linkcolor=blue,urlcolor=blue}

\begin{document}

\title{A Delayed Renewal Cage Model for Transient Non-Gaussian Dynamics}

\author{Zijun Lu}
\affiliation{Independent research note}

\date{\today}

\begin{abstract}
Glass-forming liquids and polymer melts often display a transient
non-Gaussian parameter (NGP) peak during the crossover from localized cage
motion to long-time transport. A harmonic Langevin cage explains the
mean-squared-displacement (MSD) plateau but remains Gaussian and therefore
cannot generate a nonzero NGP. Here we introduce a minimal delayed renewal
cage model in which local cage motion is interrupted by delayed discrete
cage-center renewal events, rather than by a continuously fluctuating
diffusivity. The model yields closed-form expressions for the MSD and NGP,
\begin{equation}
  \langle \Delta x^2(t) \rangle = L(t)+qR(t), \qquad
  \alpha_2(t)=\frac{q^2R(t)}{[L(t)+qR(t)]^2},
\end{equation}
where $L(t)$ is the local cage variance, $q$ is the jump variance per renewal
event, and $R(t)$ is the mean delayed renewal count. With renewal intensity
$r(t)=\lambda[1-\exp(-t/\tau_d)]^2$, the NGP starts from zero, develops a
finite-time maximum, and decays as $1/(\lambda t)$ at long times. In the
plateau regime the peak condition reduces to $qR(t^\ast)=A$, giving
$\alpha_2(t^\ast)=q/(4A)$. This also gives observable peak diagnostics,
including $q/A\simeq 4\alpha_2(t^\ast)$ and
$R(t^\ast)\simeq 1/[4\alpha_2(t^\ast)]$, plus a finite-time consistency check
that compares the peak-inferred renewal rate with a late-time NGP inversion.
The same renewal generator gives a closed-form self-intermediate scattering
function with a cage plateau and an alpha-relaxation rate. A minimal
temperature-dependent extension predicts when diffusion and structural
relaxation decouple through the product $\lambda(T)\tau_d(T)$ and gives a
closed time-temperature-superposition residual for the alpha shape. The delayed
renewal count regularizes the short-time behavior of jump models while
preserving analytic tractability and long-time Gaussian recovery. A
finite-exchange gamma extension then gives a closed criterion for
stretched-like alpha decay without leaving residual long-time non-Gaussianity,
and its predictions are contrasted with a static gamma-disorder null model. A
persistence-exchange extension separates first cage escape from subsequent
exchange, producing Stokes-Einstein decoupling at fixed long-time diffusion
while preserving Gaussian recovery. The result is an effective diagnostic
theory for dynamical glass signatures, with thermodynamic glass-transition
features included only through an explicit entropy closure rather than a
microscopic derivation of an ideal glass transition.
\end{abstract}

\maketitle

\section{Introduction}

Dynamic heterogeneity near glass transition is commonly visible through a
transient non-Gaussian parameter and broad tails in the self van Hove
distribution. A single harmonic Langevin cage gives an MSD plateau but remains
Gaussian. Conversely, diffusing-diffusivity and superstatistical models
generate non-Gaussian diffusion through fluctuating mobility
\cite{chubynsky2014diffusing,chechkin2017brownian,guan2014fickian}, and such
ideas have been tested in glass-forming liquids \cite{rusciano2022fickian},
with subsequent discussion about whether the Fickian non-Gaussian language is
the best description of supercooled-liquid heterogeneity
\cite{berthier2023comment,rusciano2022reply}. Cage rearrangements and
cage-size precursors are also directly visible in colloidal and simulated
glass-formers \cite{weeks2002cage,pastore2017cage}. The purpose of this note
is narrower: construct a closed-form renewal-cage model that connects local
caging, delayed cage rearrangement, an NGP peak, a self van Hove tail,
self-intermediate scattering relaxation, and long-time Gaussian recovery. The
non-Gaussianity here is not assigned to a continuously fluctuating diffusivity;
it is generated by the delayed, discrete renewal count of cage-center jumps.

\section{Model}

The displacement is decomposed into local cage motion plus cage-center jumps,
\begin{equation}
  \Delta x(t) = \xi_{\rm cage}(t) + \sum_{j=1}^{N(t)} \eta_j.
\end{equation}
The local cage contribution is Gaussian with variance
\begin{equation}
  L(t)=A\left[1-\exp(-t/\tau_c)\right].
\end{equation}
The cage-center jumps $\eta_j$ are independent Gaussian increments with
variance $q$. The renewal count $N(t)$ is an inhomogeneous Poisson process with
intensity
\begin{equation}
  r(t)=\lambda\left[1-\exp(-t/\tau_d)\right]^2.
\end{equation}
The corresponding mean renewal count is
\begin{equation}
  R(t)=\lambda\left[
    t-2\tau_d(1-e^{-t/\tau_d})
    +\frac{\tau_d}{2}(1-e^{-2t/\tau_d})
  \right].
\end{equation}
The delayed onset gives $R(t)\sim \lambda t^3/(3\tau_d^2)$ as $t\to0$.
The square in Eq.~(4) is not essential for the closed-form moment
calculation, but it is the minimal integer exponent that makes the NGP start
continuously from zero. More generally, if
$r_m(t)=\lambda[1-\exp(-t/\tau_d)]^m$, then
\begin{equation}
  R_m(t)\sim \frac{\lambda t^{m+1}}{(m+1)\tau_d^m},\qquad t\to0.
\end{equation}

\section{Moments and Non-Gaussian Parameter}

Conditional on $N(t)=n$, the displacement is Gaussian with variance
\begin{equation}
  V(t|n)=L(t)+nq.
\end{equation}
Since $N(t)$ is Poisson, $\mathbb{E}[V]=L+qR$ and
$\mathrm{Var}(V)=q^2R$. Thus
\begin{align}
  M_2(t) &= L(t)+qR(t),\\
  M_4(t) &= 3\left([L(t)+qR(t)]^2+q^2R(t)\right).
\end{align}
The one-dimensional NGP is
\begin{equation}
  \alpha_2(t)=\frac{M_4(t)}{3M_2(t)^2}-1
  =\frac{q^2R(t)}{[L(t)+qR(t)]^2}.
\end{equation}
For isotropic three-dimensional displacements, conditional Gaussianity gives
$\langle r^2|V\rangle=3V$ and $\langle r^4|V\rangle=15V^2$. Hence the standard
three-dimensional NGP,
\begin{equation}
  \alpha_2^{3D}(t)=\frac{3\langle r^4\rangle}{5\langle r^2\rangle^2}-1,
\end{equation}
reduces to the same expression.

\section{Self-Intermediate Scattering Function}

The same renewal structure gives a closed-form self-intermediate scattering
function. For a fixed renewal count, the displacement remains Gaussian, so
\begin{equation}
  F_s(k,t|N)=
  \exp\left[-\frac{k^2}{2}\left(L(t)+qN(t)\right)\right].
\end{equation}
Taking the Poisson average gives
\begin{equation}
  F_s(k,t)=
  \exp\left[
    -\frac{k^2L(t)}{2}
    +R(t)\left(e^{-k^2q/2}-1\right)
  \right].
\end{equation}
This separates the cage Debye-Waller factor from the renewal-controlled alpha
decay,
\begin{equation}
  F_s(k,t)=
  \exp[-k^2L(t)/2]\,
  \Phi_\alpha(k,t),
  \qquad
  \Phi_\alpha(k,t)=
  \exp\left[-\Gamma_k R(t)\right],
\end{equation}
where $\Gamma_k=1-e^{-k^2q/2}$. Thus the cage plateau is approximately
$f_k=\exp(-k^2A/2)$ once $L(t)\simeq A$ but before appreciable renewal, while
the long-time alpha-relaxation rate is
\begin{equation}
  \tau_\alpha(k)^{-1}\simeq
  \lambda\left(1-e^{-k^2q/2}\right).
\end{equation}
For small $k$, $\tau_\alpha(k)^{-1}\simeq \lambda k^2q/2$, matching ordinary
diffusive scaling with $D=\lambda q/2$ in one dimension. For larger $k$, the
rate saturates at $\lambda$, reflecting the renewal-count bottleneck rather
than a continuously varying diffusivity.
The same expression links the alpha time to the NGP peak without adding a
parameter. If the alpha time is defined by
$\Phi_\alpha(k,\tau_\alpha)=h$, then
\begin{equation}
  R_\alpha(k,h)
  =\frac{-\log h}{\Gamma_k},
  \qquad
  R_\ast=\frac{A}{q}.
\end{equation}
Therefore
\begin{equation}
  \frac{R_\alpha}{R_\ast}
  =
  \frac{-q\log h}
       {A\left(1-e^{-k^2q/2}\right)},
  \qquad
  \frac{\tau_\alpha}{t^\ast}
  =
  \frac{R^{-1}(R_\alpha)}{R^{-1}(R_\ast)} ,
\end{equation}
where the inverse is taken with the same delayed-renewal clock $R(t)$. This
gives a direct test of whether the NGP peak and structural relaxation are
controlled by one renewal process.

\section{Observable Inversion and Falsifiability}

The same formulas can be read in the reverse direction. Suppose a simulation or
experiment supplies the cage plateau $f_k$, long-time diffusion coefficient
$D$, cage-normalized alpha time $\tau_\alpha(k)$ at threshold $h$, and an
independent estimate of the delayed onset time $\tau_d$. Then
\begin{equation}
  A=-\frac{2\log f_k}{k^2}
\end{equation}
and $D=\lambda q/2$. The alpha-time equation gives
\begin{equation}
  \lambda\tau_dF(\tau_\alpha/\tau_d)
  \left(1-e^{-k^2q/2}\right)=-\log h.
\end{equation}
Eliminating $\lambda$ yields a scalar equation for $q$,
\begin{equation}
  \frac{1-e^{-k^2q/2}}{q}=
  \frac{-\log h}{2D\tau_dF(\tau_\alpha/\tau_d)}.
\end{equation}
Because the left side is bounded above by $k^2/2$, the delayed-renewal model
has a positive-jump solution only if
\begin{equation}
  \mathcal{M}_{\rm inv}\equiv
  \frac{D\tau_dF(\tau_\alpha/\tau_d)k^2}{-\log h}>1.
\end{equation}
This is a direct data-level falsification test: if the measured
$(D,\tau_\alpha,\tau_d,k)$ fail this inequality, no choice of positive jump
variance can make the minimal model reproduce both transport and scattering.
When the inequality holds, the inferred $q$ gives $\lambda=2D/q$, and the NGP
peak is no longer adjustable:
\begin{equation}
  R(t^\ast)=\frac{A}{q},\qquad
  \alpha_2(t^\ast)=\frac{q}{4A}.
\end{equation}
Thus the NGP peak time and height become out-of-sample checks on the parameters
inferred from $F_s(k,t)$ and long-time diffusion.

One can also invert the observables without supplying $\tau_d$ externally. Use
$f_k$ to infer $A$, the NGP peak height to infer
\begin{equation}
  q=4A\alpha_2(t^\ast),
\end{equation}
and diffusion to infer $\lambda=2D/q$. The peak time then fixes the delayed
onset by solving
\begin{equation}
  \lambda\tau_d F(t^\ast/\tau_d)=\frac{A}{q}.
\end{equation}
Equivalently, with $s=t^\ast/\tau_d$,
\begin{equation}
  \frac{F(s)}{s}=\frac{A/q}{\lambda t^\ast}.
\end{equation}
The existence condition is $(A/q)/(\lambda t^\ast)<1$. After this inversion,
$\tau_\alpha(k)$ is a held-out consistency check rather than an input parameter.

\section{Temperature Dependence and Stokes-Einstein Decoupling}

The fixed-parameter model can be turned into a minimal temperature diagnostic
by assigning reduced-temperature dependence to its effective parameters. Let
\begin{equation}
  \Delta_T=\frac{1}{T}-\frac{1}{T_0}.
\end{equation}
We use the phenomenological laws
\begin{align}
  \lambda(T)&=\lambda_0 e^{-E_\lambda\Delta_T},&
  \tau_d(T)&=\tau_{d0} e^{E_d\Delta_T},\\
  A(T)&=A_0 e^{-E_A\Delta_T},&
  \frac{q(T)}{A(T)}&=\beta_0 e^{E_\beta\Delta_T}.
\end{align}
They encode four standard cooling trends without claiming a microscopic
derivation: renewal events become rarer, the delayed cage-breaking onset grows,
the local cage stiffens, and the jump-to-cage contrast may increase.

The long-time diffusion coefficient per coordinate is
\begin{equation}
  D(T)=\frac{\lambda(T)q(T)}{2}.
\end{equation}
The cage-normalized self-intermediate scattering function is
\begin{equation}
  \Phi_\alpha(k,t;T)=\exp[-\Gamma_k(T)R(t;T)].
\end{equation}
The $1/e$ alpha time is defined by
\begin{equation}
  \Gamma_k(T)R(\tau_\alpha;T)=1,\qquad
  \Gamma_k(T)=1-e^{-k^2q(T)/2}.
\end{equation}
Using $R(t;T)=\lambda(T)\tau_d(T)F[t/\tau_d(T)]$ gives the closed diagnostic
\begin{equation}
  \tau_\alpha(k,T)=
  \tau_d(T)\,
  F^{-1}\left(
    \frac{1}{\Gamma_k(T)\lambda(T)\tau_d(T)}
  \right).
\end{equation}
Therefore
\begin{equation}
  D(T)\tau_\alpha(k,T)=
  \frac{\lambda(T)q(T)\tau_d(T)}{2}\,
  F^{-1}\left(
    \frac{1}{\Gamma_k(T)\lambda(T)\tau_d(T)}
  \right).
\end{equation}
This expression is the model's Stokes-Einstein diagnostic. In the late-renewal
limit it reduces approximately to $q/(2\Gamma_k)$, so changing $\lambda$ alone
does not produce a strong violation. A violation appears when cooling increases
the delayed-onset product $\lambda(T)\tau_d(T)$ and/or the jump-to-cage contrast
fast enough that structural relaxation is bottlenecked by delayed cage renewal
more strongly than long-time diffusion.
The same scan gives the local fractional Stokes-Einstein exponent
\begin{equation}
  \xi_{\rm SE}(T)=
  -\frac{d\log D(T)}{d\log\tau_\alpha(k,T)},
  \qquad D\propto\tau_\alpha^{-\xi_{\rm SE}}.
\end{equation}
Ordinary Stokes-Einstein scaling corresponds to $\xi_{\rm SE}=1$, whereas
decoupled glassy transport gives $0<\xi_{\rm SE}<1$.
The apparent activation energy of the structural relaxation is
\begin{equation}
  E_{\rm app}(T)=
  \frac{d\log \tau_\alpha(k,T)}{d(1/T)}.
\end{equation}
For Arrhenius relaxation this is constant; growth of $E_{\rm app}$ on cooling
is the minimal fragility signal produced by the temperature-dependent delayed
renewal law. A local Angell-style fragility proxy is
$m_{\rm loc}(T)=E_{\rm app}(T)/(T\log 10)$.

The same equations give a closed diagnostic for time-temperature
superposition of the alpha relaxation. Define the scaled alpha-shape function
\begin{equation}
  Y_k(u;T)=
  \frac{-\log\Phi_\alpha(k,u\tau_\alpha;T)}{-\log h}.
\end{equation}
Using the delayed-renewal form gives
\begin{equation}
  Y_k(u;T)=
  \frac{\Gamma_k(T)\lambda(T)\tau_d(T)}
       {-\log h}
  F\left[u\,s_\alpha(T)\right],
  \qquad
  s_\alpha(T)=\frac{\tau_\alpha(k,T)}{\tau_d(T)}
  =
  F^{-1}\left[
    \frac{-\log h}{\Gamma_k(T)\lambda(T)\tau_d(T)}
  \right].
\end{equation}
Thus the alpha shape after scaling time by $\tau_\alpha$ is controlled by the
single variable
\begin{equation}
  C_k(T)=\Gamma_k(T)\lambda(T)\tau_d(T).
\end{equation}
Exact time-temperature superposition within this minimal model requires
$C_k(T)$ to be temperature independent. A data-level residual can be defined on
a chosen scaled-time grid $\{u_i\}$ as
\begin{equation}
  \epsilon_{\rm TTS}(T)=
  \left[
    \frac{1}{n}\sum_i
    \left(
      \log Y_k(u_i;T)-\log Y_k(u_i;T_{\rm ref})
    \right)^2
  \right]^{1/2}.
\end{equation}
This criterion is complementary to the Stokes-Einstein diagnostic: it tests the
shape of the alpha relaxation after removing its time scale, not only the
growth of $\tau_\alpha$.

\section{Activated-Barrier Interpretation and Dynamic Heterogeneity}

The temperature law can be read as a minimal activated-barrier reduction. If
the long-time renewal rate is controlled by a cage-breaking barrier
$E_\lambda$ while the delayed onset is controlled by a larger local persistence
barrier $E_d$, then
\begin{equation}
  \lambda(T)\tau_d(T)
  =\lambda_0\tau_{d0}\exp[(E_d-E_\lambda)\Delta_T].
\end{equation}
Thus the barrier gap $E_d-E_\lambda$ is the microscopic parameter that makes
the delayed-renewal control variable grow on cooling. This gives a direct
mechanism for the Stokes-Einstein diagnostic above: diffusion is set by the
asymptotic renewal rate and jump length, while structural relaxation is also
penalized by the delayed cage-breaking onset.

The same renewal-count fluctuations give a closed single-particle analogue of
the four-point susceptibility. Define
\begin{equation}
  W_k(t|N)=\exp[-k^2L(t)/2]a_k^{N(t)},\qquad
  a_k=e^{-k^2q/2}.
\end{equation}
Then $F_s(k,t)=\mathbb{E}[W_k(t|N)]$ and the renewal contribution to the
self-scattering variance is
\begin{equation}
  \chi_R(k,t)=\mathrm{Var}_N[W_k(t|N)]
  =e^{-k^2L(t)}
  \left[
    e^{R(t)(a_k^2-1)}-e^{2R(t)(a_k-1)}
  \right].
\end{equation}
Equivalently,
\begin{equation}
  \frac{\chi_R(k,t)}{F_s(k,t)^2}
  =\exp[R(t)(a_k-1)^2]-1.
\end{equation}
This is not a full spatial $\chi_4(t)$ because spatial correlations between
different particles have been integrated out. It is the renewal-count component:
it peaks when the number of cage renewals is most heterogeneous and can be
compared with the NGP and $F_s$ relaxation times in simulation trajectories.
To connect this single-particle renewal variance to a conventional four-point
susceptibility, suppose that a correlated renewal domain contains $N_{\rm corr}$
particles sharing the same renewal-count history. Then the renewal contribution
to the per-particle four-point susceptibility is
\begin{equation}
  \chi_4^R(k,t)=N_{\rm corr}\chi_R(k,t).
\end{equation}
Thus an observed renewal-dominated four-point peak gives the cooperative-size
estimator
\begin{equation}
  N_{\rm corr}=
  \frac{\chi_{4,\mathrm{peak}}^{\mathrm{obs}}}
       {\max_t \chi_R(k,t)}.
\end{equation}
This does not replace a spatial four-point calculation, but it gives a closed
conversion between the analytically predicted renewal fluctuation and a measured
dynamic-heterogeneity amplitude.
A minimal spatial closure can make $N_{\rm corr}$ clock-derived rather than
externally chosen. Suppose facilitation spreads diffusively during the local
persistence time $\tau_p$ with effective diffusivity $D_f$ in dimension $d$.
Then
\begin{equation}
  \xi_4(T)=\left[\ell_0^2+2dD_f\tau_p(T)\right]^{1/2},
  \qquad
  N_{\rm corr}(T)=\rho V_d\xi_4(T)^d,
\end{equation}
where $V_d=\pi^{d/2}/\Gamma(1+d/2)$ is the unit-ball volume and $\rho$ is the
number density. In the present delayed-renewal reduction we use
$\tau_p(T)\simeq\tau_d(T)$, so cooling that increases the persistence onset
also grows a dynamic length and hence the four-point amplitude. This is still a
compact-domain closure, not a microscopic spatial facilitation field, but it
moves the $\chi_4$ amplitude from an arbitrary scale factor to a falsifiable
clock-derived prediction.
The inverse test is equally simple:
\begin{equation}
  D_f(T)=\frac{\xi_4(T)^2-\ell_0^2}{2d\tau_p(T)}.
\end{equation}
Thus spatial data reject this closure if the $D_f(T)$ values inferred from
different temperatures do not collapse to a positive constant within
uncertainty. This gives a concrete four-point-length protocol without claiming
to derive the full spatial facilitation field.

\section{Thermodynamic Entropy Closure}

The renewal model above is kinetic. To compare with thermodynamic glass
signatures, one can add a minimal configurational-entropy sector rather than
pretending it was already present. Following the Kauzmann extrapolation
\cite{kauzmann1948nature}, take
\begin{equation}
  s_c(T)=s_{c0}\frac{T-T_K}{T_0-T_K},
\end{equation}
with $s_c(T_K)=0$. The corresponding excess heat capacity is
\begin{equation}
  \Delta c_p(T)=T\frac{ds_c}{dT}.
\end{equation}
The Adam-Gibbs relation \cite{adam1965temperature} then gives
\begin{equation}
  \tau_{\rm AG}(T)=\tau_0
  \exp\left[
    \frac{A_{\rm AG}}{T s_c(T)}
    -\frac{A_{\rm AG}}{T_0 s_c(T_0)}
  \right].
\end{equation}
In this closure, $\tau_{\rm AG}(T)$ sets the delayed-renewal persistence time
$\tau_d(T)$ while the renewal rate and cage parameters can be kept fixed or
combined with the dynamic laws above. This produces a thermodynamic route to
super-Arrhenius alpha slowdown and a Kauzmann-like entropy extrapolation. It is
still an added effective entropy law, not a microscopic derivation of
configurational entropy or a proof of an ideal glass transition; those are the
remaining RFOT-level questions
\cite{lubchenko2007theory,berthier2011theoretical}.

\section{Effective MCT Beta-Window Closure}

The cage plateau in the renewal model does not by itself reproduce the full
mode-coupling-theory beta-relaxation structure. To make this limitation
testable rather than hidden, we add a minimal beta-window envelope around a
normalized correlator plateau $f_c$,
\begin{equation}
  \phi_\beta(t)=
  \begin{cases}
    f_c+h_a(t/t_\beta)^{-a}, & t<t_\beta,\\
    f_c-h_b(t/t_\beta)^b, & t\ge t_\beta.
  \end{cases}
\end{equation}
The first branch is the critical approach to the plateau and the second is the
von Schweidler departure \cite{gotze1992relaxation,kob1995intermediate}. In
idealized MCT the exponents satisfy a common exponent-parameter relation,
\begin{equation}
  \lambda_a=\frac{\Gamma(1-a)^2}{\Gamma(1-2a)},\qquad
  \lambda_b=\frac{\Gamma(1+b)^2}{\Gamma(1+2b)}.
\end{equation}
Therefore the mismatch $|\lambda_a-\lambda_b|$ is a direct diagnostic of
whether a fitted beta window is internally MCT-consistent. Cooling is encoded by
a beta-clock law $t_\beta(T)=t_{\beta0}\exp[E_\beta\Delta(1/T)]$ and by a
separate alpha time $\tau_\alpha(T)$, so the generated observable
$\tau_\alpha/t_\beta$ measures beta-alpha time-scale separation. This closure
upgrades the theory from merely having a cage plateau to reproducing the
two-sided beta-window power laws, but it remains an effective diagnostic
closure; the microscopic memory kernel and material-specific exponent parameter
are still outside the renewal derivation.

\section{Finite-Exchange Heterogeneity Extension}

The Poisson renewal model is the minimal clock, but glass-forming systems also
show trajectory-to-trajectory mobility heterogeneity. Before introducing
exchange, it is useful to state the static mobility-disorder null model. Let a
trajectory have a fixed dimensionless mobility $M$ drawn from a gamma
distribution with mean one and shape $\kappa_0$, and let
$N(t)|M$ be Poisson with mean $MR(t)$. Then
\begin{equation}
  G_N^{\rm st}(z;t)=
  \left[1+\frac{R(t)(1-z)}{\kappa_0}\right]^{-\kappa_0},
\end{equation}
\begin{equation}
  \mathbb{E}N(t)=R(t),\qquad
  {\rm Var}\,N(t)=R(t)+\frac{R(t)^2}{\kappa_0}.
\end{equation}
Consequently,
\begin{equation}
  \alpha_2^{\rm st}(t)=
  \frac{q^2[R(t)+R(t)^2/\kappa_0]}{[L(t)+qR(t)]^2}
  \longrightarrow \frac{1}{\kappa_0},
\end{equation}
and
\begin{equation}
  \Phi_\alpha^{\rm st}(k,t)=
  \left[1+\frac{\Gamma_kR(t)}{\kappa_0}\right]^{-\kappa_0},
  \qquad
  -\frac{\log\Phi_\alpha^{\rm st}}{R(t)}\longrightarrow0.
\end{equation}
Thus static gamma disorder can broaden alpha relaxation, but it predicts a
nonzero long-time NGP plateau and an alpha decay that becomes subexponential in
renewal count. It therefore cannot explain stretched-like alpha relaxation and
long-time Gaussian recovery simultaneously.

A minimal closed correction is to let the renewal count be gamma mixed over a
finite exchange window. Conditional
on a dimensionless mobility $M$, take $N(t)|M$ to be Poisson with mean
$MR(t)$, and let the gamma shape self-average as
\begin{equation}
  \kappa_{\rm eff}(t)=
  \kappa_0\left[1+\frac{R(t)}{R_x}\right].
\end{equation}
The renewal-count probability-generating function is then the negative-binomial
form
\begin{equation}
  G_N(z;t)=
  \left[
    1+\frac{R(t)(1-z)}{\kappa_{\rm eff}(t)}
  \right]^{-\kappa_{\rm eff}(t)} .
\end{equation}
It preserves the mean renewal count but adds finite-time overdispersion,
\begin{equation}
  \mathbb{E}N(t)=R(t),\qquad
  {\rm Var}\,N(t)=R(t)+\frac{R(t)^2}{\kappa_{\rm eff}(t)}.
\end{equation}
Since $\kappa_{\rm eff}(t)\sim \kappa_0R(t)/R_x$ at long times, the relative
count variance still decays as $1/R(t)$ and Gaussian recovery is retained.
The NGP and cage-normalized alpha decay become
\begin{equation}
  \alpha_2^{\rm hx}(t)=
  \frac{q^2{\rm Var}\,N(t)}
       {[L(t)+qR(t)]^2},
\end{equation}
and
\begin{equation}
  \Phi_\alpha^{\rm hx}(k,t)=
  G_N(e^{-k^2q/2};t)
  =
  \left[
    1+\frac{\Gamma_kR(t)}{\kappa_{\rm eff}(t)}
  \right]^{-\kappa_{\rm eff}(t)} .
\end{equation}
The local stretching diagnostic is
\begin{equation}
  \beta_{\rm loc}(t)=
  \frac{d\log[-\log\Phi_\alpha^{\rm hx}(k,t)]}{d\log t}.
\end{equation}
Values $\beta_{\rm loc}<1$ in the alpha window are therefore produced by finite
exchange of renewal mobility, while the same construction predicts the
long-time return of $\alpha_2(t)$ toward zero.
For a window-level comparison with the experimental stretched-exponential
language, we also fit
\begin{equation}
  \Phi_\alpha^{\rm hx}(k,t)\simeq
  \exp\left[-(t/\tau_{\rm KWW})^{\beta_{\rm KWW}}\right],
\end{equation}
by regressing $\log[-\log\Phi_\alpha]$ against $\log t$ over a fixed alpha
decay window. This produces a falsifiable triple
$(\beta_{\rm KWW},\tau_{\rm KWW},\epsilon_{\rm KWW})$ rather than a qualitative
claim of stretching.
It also gives two late-time consistency tests governed by the same ratio
$c=R_x/\kappa_0$:
\begin{equation}
  R(t)\alpha_2^{\rm hx}(t)\to 1+c,
\end{equation}
and
\begin{equation}
  -\frac{\log\Phi_\alpha^{\rm hx}(k,t)}{R(t)}
  \to
  \frac{\log(1+\Gamma_k c)}{c}.
\end{equation}
Thus a late NGP amplitude estimates $c$ as $c=R\alpha_2-1$, while the long-time
alpha slope independently estimates the same $c$ by inverting
$\log(1+\Gamma_kc)/c$. Agreement between these two estimates is a falsifiable
signature of finite-exchange heterogeneity rather than static mobility
disorder.
This also defines an experimental diagnostic map. For each wave number, the
pair $(R\alpha_2,\,-\log\Phi_\alpha/R)$ places a trajectory ensemble on a
one-parameter curve indexed by $c$. Large $c$ simultaneously increases the late
NGP amplitude and slows the alpha decay per renewal; if the two inferred values
of $c$ disagree, the finite-exchange renewal mechanism is insufficient.
In practice, late-time data give the two estimates
\begin{equation}
  c_{\rm NGP}=R_\ell\alpha_2(t_\ell)-1,
  \qquad
  \frac{\log(1+\Gamma_k c_\alpha)}{c_\alpha}
  =
  -\frac{\log\Phi_\alpha(k,t_\ell)}{R_\ell}.
\end{equation}
The scalar residual
\begin{equation}
  \Delta_c=\log(c_\alpha/c_{\rm NGP})
\end{equation}
is therefore a direct finite-exchange falsification statistic. Small
$|\Delta_c|$ supports the one-parameter exchange picture; a large residual
shows that NGP recovery and alpha slowing are not controlled by the same renewal
heterogeneity.
With independent uncertainties in $R_\ell$, $\alpha_2(t_\ell)$, and
$s_\alpha=-\log\Phi_\alpha/R_\ell$, the same protocol gives a statistical
score. The propagated NGP uncertainty is
\begin{equation}
  \sigma_{c_{\rm NGP}}^2
  =
  \alpha_2(t_\ell)^2\sigma_R^2+R_\ell^2\sigma_{\alpha_2}^2,
\end{equation}
while $c_\alpha$ uses
\begin{equation}
  f'(c)=
  \frac{\Gamma_k c/(1+\Gamma_kc)-\log(1+\Gamma_kc)}{c^2},
  \qquad
  \sigma_{c_\alpha}=\frac{\sigma_{s_\alpha}}{|f'(c_\alpha)|}.
\end{equation}
Thus
\begin{equation}
  \sigma_{\Delta_c}^2=
  \left(\frac{\sigma_{c_\alpha}}{c_\alpha}\right)^2+
  \left(\frac{\sigma_{c_{\rm NGP}}}{c_{\rm NGP}}\right)^2,\qquad
  z_c=\frac{|\Delta_c|}{\sigma_{\Delta_c}}.
\end{equation}
The same construction is overdetermined when $F_s(k,t)$ is measured at several
wave numbers. Each $k$ gives an independent $c_\alpha(k)$ through
$\log(1+\Gamma_k c)/c=s_\alpha(k)$, while the late NGP gives one
$c_{\rm NGP}$. A valid finite-exchange renewal description therefore requires
the set $\{c_\alpha(k)\}$ to collapse to the same value and to agree with
$c_{\rm NGP}$ within propagated errors.

\section{Persistence-Exchange Decoupling}

The delayed Poisson clock above captures a smooth onset, but glassy dynamics
often separates the first persistence time from the subsequent exchange time
\cite{hedges2007persistence}.
To isolate that effect, let the first cage escape have mean $\tau_p$ and all
later exchanges have mean $\tau_x$. For $n\ge1$ the renewal-count distribution
can be written as
\begin{equation}
  P[N(t)=n]=
  \int_0^t \frac{e^{-(t-u)/\tau_p}}{\tau_p}
  e^{-u/\tau_x}\frac{(u/\tau_x)^{n-1}}{(n-1)!}\,du,
  \qquad
  P[N(t)=0]=e^{-t/\tau_p}.
\end{equation}
Equivalently, the count probability-generating function is closed:
\begin{equation}
  G(z,t)=\langle z^{N(t)}\rangle
  =e^{-t/\tau_p}
  +\frac{z\tau_p^{-1}\left[
    e^{(z-1)t/\tau_x}-e^{-t/\tau_p}
  \right]}
  {\tau_p^{-1}+(z-1)\tau_x^{-1}},
\end{equation}
where the removable singularity is evaluated by continuity. With
$a_k=e^{-k^2q/2}$ this gives the cage-normalized alpha relaxation directly as
\begin{equation}
  \Phi_\alpha^{px}(k,t)=G(a_k,t),
\end{equation}
and the NGP remains a count-variance diagnostic,
\begin{equation}
  \alpha_2^{px}(t)=
  \frac{q^2{\rm Var}\,N(t)}
       {[L(t)+q\langle N(t)\rangle]^2}.
\end{equation}
This extension can also be inverted from directly measurable dynamical
quantities. If $q$ is fixed from the MSD jump scale or from the preceding
peak/scattering inversion, long-time diffusion gives
\begin{equation}
  \tau_x=\frac{q}{2D}.
\end{equation}
The first-persistence time is then the solution of the scalar equation
\begin{equation}
  G(a_k,\tau_\alpha;\tau_p,\tau_x)=h,
\end{equation}
with $\tau_p\ge\tau_x$. This inversion uses $D$ and the cage-normalized
alpha time only. A late NGP measurement is therefore held out:
\begin{equation}
  \Delta_{\rm px}=
  \log\frac{\alpha_{2,\mathrm{obs}}(t_\ell)}
           {\alpha_{2,\mathrm{pred}}^{px}(t_\ell)}.
\end{equation}
Small $|\Delta_{\rm px}|$ supports a persistence/exchange interpretation,
whereas a large residual rejects it even if the same $D\tau_\alpha$ product can
be matched.
The Poisson renewal model is recovered when $\tau_p=\tau_x$. When
$\tau_p>\tau_x$, the long-time diffusion coefficient remains
$D=q/(2\tau_x)$, but the cage-normalized alpha relaxation is delayed by the
first-persistence clock. Thus $D\tau_\alpha$ grows with $\tau_p/\tau_x$ even at
fixed diffusion. This gives a sharper Stokes-Einstein diagnostic than changing
one renewal rate: alpha relaxation probes first escape, while transport probes
the post-escape exchange clock. Because $\langle N(t)\rangle\sim t/\tau_x$ and
${\rm Var}\,N(t)/\langle N(t)\rangle^2\to0$, the long-time NGP still recovers
to zero.

\section{Asymptotics and Peak Condition}

At short times,
\begin{equation}
  \alpha_2(t)\sim
  \frac{q^2\lambda\tau_c^2}{3A^2\tau_d^2}t.
\end{equation}
For the generalized delay exponent $m$ introduced above,
\begin{equation}
  \alpha_2(t)\sim
  \frac{q^2\lambda\tau_c^2}{(m+1)A^2\tau_d^m}t^{m-1}.
\end{equation}
Thus $m<1$ would give a singular origin, $m=1$ a finite nonzero NGP at the
origin, and $m>1$ a regular zero-origin NGP. The present $m=2$ choice is the
smallest integer member of this regular class and yields the linear onset in
Eq.~(12).
At long times $L(t)\to A$ and $R(t)\sim \lambda t$, so
\begin{equation}
  \alpha_2(t)\sim \frac{1}{\lambda t}.
\end{equation}
Differentiating the exact expression gives the peak condition
\begin{equation}
  R'(t)\left[L(t)-qR(t)\right]-2R(t)L'(t)=0.
\end{equation}
In the plateau regime this reduces to
\begin{equation}
  qR(t^\ast)=A,\qquad \alpha_2(t^\ast)=\frac{q}{4A}.
\end{equation}
Equivalently, a measured peak height gives the plateau-regime estimates
\begin{equation}
  \frac{q}{A}\simeq 4\alpha_2(t^\ast),\qquad
  R(t^\ast)\simeq \frac{1}{4\alpha_2(t^\ast)}.
\end{equation}
For the delayed intensity used above, write
\begin{equation}
  R(t)=\lambda\tau_d F(t/\tau_d),
\end{equation}
where
\begin{equation}
  F(s)=s-2(1-e^{-s})+\frac{1}{2}(1-e^{-2s}).
\end{equation}
If $\tau_d$ is obtained independently or fitted from the rise of the peak, the
peak time estimates the asymptotic renewal rate,
\begin{equation}
  \lambda \simeq
  \frac{1}{4\alpha_2(t^\ast)\tau_d F(t^\ast/\tau_d)}.
\end{equation}
The independent asymptotic long-time check is
$\lambda\simeq[t\,\alpha_2(t)]^{-1}$ once $t$ is beyond the cage plateau and
renewal delay. A finite-time version is obtained without taking the
$t\to\infty$ limit. Let
\begin{equation}
  \beta=\frac{q}{A}\simeq 4\alpha_2(t^\ast),\qquad y(t)=\beta R(t).
\end{equation}
Then the plateau NGP can be written as
\begin{equation}
  \alpha_2(t)=\frac{\beta y(t)}{[1+y(t)]^2},
\end{equation}
which has maximum $\beta/4$ at $y=1$. Therefore any value used in this
finite-time inversion must satisfy $\alpha_\ell\le \beta/4$. For
$0<\alpha_\ell<\beta/4$, the inverse has two branches,
\begin{equation}
  y_{\pm}=
  \frac{\beta-2\alpha_\ell\pm\sqrt{\beta^2-4\beta\alpha_\ell}}
       {2\alpha_\ell},
\end{equation}
with $y_-y_+=1$. The pre-peak branch has $y_-<1$, while the post-peak branch
has $y_+>1$. Thus a late-time NGP value $\alpha_\ell=\alpha_2(t_\ell)$ gives
\begin{equation}
  y_\ell=
  \frac{\beta-2\alpha_\ell+\sqrt{\beta^2-4\beta\alpha_\ell}}
       {2\alpha_\ell},
\end{equation}
and therefore
\begin{equation}
  \lambda_\ell\simeq
  \frac{y_\ell}{\beta\tau_d F(t_\ell/\tau_d)}.
\end{equation}
Agreement between $\lambda_\ell$ and the peak-inferred rate is a falsifiable
consistency condition using only the peak height, peak time, delay scale, and a
late NGP value.

\section{Self Van Hove Distribution}

The three-dimensional radial self van Hove distribution is a Poisson-weighted
Maxwell mixture,
\begin{equation}
  G_s(r,t)=\sum_{n=0}^\infty P[N(t)=n]
  \sqrt{\frac{2}{\pi}}\frac{r^2}{V_n(t)^{3/2}}
  \exp\left[-\frac{r^2}{2V_n(t)}\right],
\end{equation}
with $V_n(t)=L(t)+nq$. This representation shows directly why the distribution
has transient broad tails near the NGP peak and returns toward a matched
Gaussian baseline at long times.

\section{Reproducible Results}

The accompanying repository generates all figures from closed-form equations.
For the default parameter set, the NGP peak occurs at $t^\ast=11.306$ with
$\alpha_2(t^\ast)=0.200$. At $t=180$, the NGP has decayed to $0.0293$. The
tail probability beyond $r=5$ at peak time is about $2.90$ times the matched
Gaussian tail, while the long-time ratio returns to about $0.97$. The
finite-time consistency inversion at $t=180$ gives
$\lambda_\ell=0.180000$, while the peak diagnostic gives
$\lambda_\ast=0.180010$; the simpler asymptotic estimate
$[t\alpha_2(t)]^{-1}$ gives $0.189664$ at the same finite time.
For the illustrative temperature law, cooling from $T=1.00$ to $T=0.62$
decreases $D$ by a factor of $3.30$, increases $\tau_\alpha(k=1.1)$ by a
factor of $6.72$, and raises the normalized product $D\tau_\alpha$ to $2.03$.
The corresponding local fractional Stokes-Einstein exponent decreases from
$\xi_{\rm SE}=0.725$ at the hot end to $0.568$ at the cold end, while the
apparent alpha-relaxation activation energy grows from $E_{\rm app}=2.69$ to
$3.43$ and the local fragility proxy grows from $1.17$ to $2.41$.
For the same scan, the alpha-shape control $C_k=\Gamma_k\lambda\tau_d$ rises
from $0.207$ at $T=1.00$ to $1.334$ at $T=0.62$; the scaled alpha-shape
residual relative to the hot curve rises from $0$ to $0.611$. Thus the model
predicts not only a growing $\tau_\alpha$ but a quantifiable breakdown of
time-temperature superposition when the delayed-renewal control variable
changes on cooling.
A facilitated finite-exchange law with cooling-broadened mobility dispersion
and slower exchange raises $c=R_x/\kappa_0$ from $25.0$ to $290.2$ over the same
temperature interval, increasing the late $R\alpha_2$ amplitude from $26.0$ to
$291.2$ while reducing the alpha-rate renormalization from $0.246$ to $0.0416$.
Increasing the activated barrier gap $E_d-E_\lambda$ from $0$ to $4.5$ raises
the cold-end normalized $D\tau_\alpha$ from $1.01$ to $3.30$. The renewal
susceptibility for $k=1.1$ peaks at $t=12.46$, close to the NGP peak time.
The observable inversion gives an existence margin
$\mathcal{M}_{\rm inv}=1.26$ for the default parameters; reducing $D$ below
about $0.79$ of its consistent value violates the scattering-transport
existence criterion.
The full inversion using $f_k$, $D$, $t^\ast$, and $\alpha_2(t^\ast)$ recovers
$A=1.00$, $q=0.80$, $\lambda=0.18$, and $\tau_d=3.00$; its held-out
$\tau_\alpha$ residual is below $10^{-12}$ in logarithmic units.
For $k=1.1$ and $h=e^{-1}$, the alpha target is $R_\alpha=2.606$ renewals,
whereas the NGP peak requires $R_\ast=1.250$ renewals; the resulting
$\tau_\alpha/t^\ast$ is $1.678$.
A synthetic four-point peak generated with $N_{\rm corr}=12$ is inverted back to
$N_{\rm corr}=12.0$ using the renewal $\chi_R$ peak, illustrating how the model
can translate a measured $\chi_4$ amplitude into a renewal-domain size.
For the finite-exchange heterogeneity example with
$\kappa_0=0.4$ and $R_x=10$, the NGP peak rises to $1.195$ at $t=32.07$, the
alpha-window median local stretching exponent is $\beta_{\rm loc}=0.805$, and
the NGP recovers to $0.00480$ by $t=3.0\times10^4$.
The same parameters give $c=R_x/\kappa_0=25$, predict the late product
$R\alpha_2\to26.0$, and reduce the asymptotic alpha decay per renewal from
$\Gamma_{1.1}=0.384$ to $0.0944$; the alpha-rate inversion recovers $c=25.0$.
Scanning $c$ shows that the joint observable window begins near $c=2$ for the
default thresholds $R\alpha_2\ge3$ and alpha-rate renormalization below $0.75$.
The late-observable protocol at $t_\ell=3.0\times10^4$ gives
$c_{\rm NGP}=24.94$, $c_\alpha=25.00$, and $\Delta_c=0.0023$ for the consistent
finite-exchange example. Replacing only the alpha slope with one corresponding
to $c_\alpha=2$ gives $\Delta_c=-2.52$ and fails the same criterion.
With $1\%$ uncertainties in $R_\ell$ and $\alpha_2(t_\ell)$ and
$\sigma_{s_\alpha}=0.002$, the corresponding scores are $z_c=0.062$ and
$z_c=76.4$, respectively.
For the multi-$k$ collapse test at $k=0.6,1.1,1.8$, consistent alpha slopes give
a weighted mean $c_\alpha=25.00$ and collapse score $z=0.096$. For a mismatch in
which only the $k=1.1$ alpha slope corresponds to $c=2$, the weighted mean drops
to $2.23$, the collapse score rises to $61.1$, and the reduced chi-square across
wave numbers is $756$.
The static-gamma null with the same $\kappa_0=0.4$ gives an instructive
contrast: at $t=3.0\times10^4$, it leaves
$\alpha_2^{\rm st}=2.499$, while finite exchange has already recovered to
$\alpha_2^{\rm hx}=0.00480$. The static alpha slope per renewal has fallen to
$6.34\times10^{-4}$, whereas finite exchange remains close to its closed
late-time value $0.0944$. This separates exchange-controlled stretching from
permanent mobility disorder.
The glass-dynamics audit then applies the same equations to a wider list of
standard dynamical signatures. Over the cooling scan, the model supports
diffusion slowdown, alpha slowdown, NGP peak shift, Stokes-Einstein violation,
fragility growth, heterogeneity growth, stretched alpha relaxation, $\chi_4$
peak growth, and Gaussian recovery, while the thermodynamic-transition flag is
zero by construction. The associated barrier-facilitation scan shows that an
exchange barrier sum of $2$ is already sufficient for the joint
dynamic-signature criterion in this parameter set, whereas no thermodynamic transition is
inferred. The closed barrier-threshold inversion gives minimum controls
$E_d-E_\lambda=3.0$ and $E_{\rm hx}=4.0$ for the target growth factors used in
the scan.
With the diffusive spatial-facilitation closure and $D_f=0.04$, cooling from
$T=1.00$ to $0.62$ increases the clock-derived $\xi_4$ by a factor of $3.09$
and $N_{\rm corr}$ by a factor of $29.5$, raising the renewal contribution to
the $\chi_4$ peak by a factor of $34.7$ without fitting an external domain
size.
The thermodynamic closure with $T_K=0.45$ and $s_c(T_0)=1.2$ drives an
Adam-Gibbs renewal delay that grows by more than four orders of magnitude by
$T=0.52$, while $s_c$ extrapolates toward zero and $\Delta c_p$ remains
positive.
The beta-window closure with $a=0.32$ and $b=0.60$ gives internally consistent
power-law slopes around the cage plateau; over the same cooling interval
$t_\beta$ grows by a factor of $9.17$ while the imposed alpha clock grows by a
factor of $101$, increasing $\tau_\alpha/t_\beta$ from $7.5$ to $82.7$.
The persistence-exchange extension keeps $D=0.35$ fixed while increasing
$\tau_p/\tau_x$ from $1$ to $12$; the resulting $D\tau_\alpha$ grows from
$1.01$ to $4.98$, while the late NGP falls from $1.21\times10^{-2}$ to
$1.21\times10^{-3}$ at the diagnostic late time. A synthetic inversion case
with $\tau_p/\tau_x=9$ recovers $\tau_x=1.00$, $\tau_p=9.00$, and predicts the
held-out late NGP $1.56\times10^{-3}$ with zero log residual; replacing only
the late NGP fails this residual check. This separates Stokes-Einstein
violation from permanent non-Gaussianity.
A joint inversion protocol using one anchor alpha time, diffusion, and the same
late NGP check recovers $\tau_p/\tau_x=8.00$ in the consistent synthetic case;
the held-out multi-$k$ alpha residual is below $10^{-8}$, the
Stokes-Einstein product grows by a factor $3.56$ over the Poisson baseline, and
the single-domain $\chi_4$ proxy grows by a factor $2.58$. Deliberately
changing only the $k=1.6$ alpha time fails the multi-$k$ residual threshold,
giving a direct falsification mode for future trajectory-level inversions.
Adding $5\%$ alpha-time errors, an $8\%$ late-NGP error, and a $10\%$
$\chi_4$-peak error turns the same protocol into a $z$-score test: the
consistent case has negligible $z$ scores, while doubling only the observed
$\chi_4$ peak gives $z=7.27$ and fails a $z<2$ criterion.

The generated file \texttt{renewal\_cage\_sota\_comparison.csv} records a
scope audit against representative experimental and simulation benchmarks.
The supported dynamical signatures include cage localization and rearrangements
\cite{weeks2002cage,pastore2017cage}, non-Gaussian van Hove tails and NGP
peaks in glass-former benchmarks \cite{kob1995vanhove,guan2014fickian,
rusciano2022fickian}, self-intermediate-scattering alpha relaxation
\cite{kob1995intermediate}, stretched-like relaxation and Stokes-Einstein
decoupling \cite{ediger2000spatial,berthier2011theoretical}, and
persistence/exchange decoupling \cite{hedges2007persistence}. The same audit
marks remaining gaps explicitly: the spatial four-point length is now a
diffusive-front closure rather than a full facilitation-field theory
\cite{lacevic2003fourpoint,berthier2024experimental}; the thermodynamic sector
is an Adam-Gibbs/Kauzmann closure rather than a microscopic entropy derivation;
and the MCT beta window is an effective two-exponent closure rather than a
derived memory-kernel calculation.
The benchmark-consistency table therefore includes a separate thermodynamic
scope row: heat-capacity and Kauzmann signatures are treated as observed
benchmarks, while the renewal dynamics sets
\texttt{dynamic\_model\_derives\_entropy}=0; Adam-Gibbs slowdown is generated
only after the explicit entropy closure is supplied.
The companion file
\texttt{renewal\_cage\_sota\_benchmark\_consistency.csv} begins converting
these literature statements into explicit checks. The first row checks cage
localization from the Debye-Waller plateau: the generated plateau has
$f_{\rm DW}=0.546$, renewal-jump contribution only $0.0055$ of the plateau MSD,
and $\tau_\alpha/\tau_{\rm cage}=27.1$, satisfying localization and
alpha-cage time-scale-separation thresholds. For the Kob-Andersen
intermediate-scattering benchmark \cite{kob1995intermediate}, the reported
qualitative pattern is that the late beta regime is well represented by a von
Schweidler law, whereas the early beta regime does not show a clear critical
decay. The beta-window visibility diagnostic encodes this as an observation
window: with only $0.071$ decades of pre-plateau critical window but $1.90$
decades of post-plateau von-Schweidler window, the model predicts hidden
critical decay and visible von-Schweidler departure, matching both qualitative
claims. A companion exponent row checks the MCT internal-consistency relation:
for $a=0.32$ and $b=0.60$, it gives $\lambda_a=0.716$ and
$\lambda_b=0.725$, a $1.15\%$ relative mismatch below the $5\%$ diagnostic
threshold. Another row encodes the long-time Gaussian-recovery benchmark: the
finite-exchange model gives $\alpha_2(t_\ell)=4.80\times10^{-3}$ below the
chosen recovery threshold $0.05$, while the static gamma mobility-disorder null
gives $\alpha_2(t_\ell)=2.50$ and therefore fails the same criterion. These are
followed by an NGP peak-shift row: over the same cooling scan the predicted
peak time moves from $11.31$ to $84.51$, a factor of $7.47$, while the peak
height grows by a factor of $1.17$ and the late NGP remains
$4.80\times10^{-3}$. A fractional Stokes-Einstein row then checks that
$D\tau_\alpha$ grows by a factor of $2.03$ and the cold-end fractional exponent
is $\xi_{\rm SE}=0.581$, satisfying the benchmark pattern
$0<\xi_{\rm SE}<1$ with a growing Stokes-Einstein product. A
dynamic-heterogeneity row checks dynamic heterogeneity: the
spatial-facilitation closure gives
$\xi_4$ growth $3.09$, $N_{\rm corr}$ growth $29.5$, and $\chi_4$ peak growth
$34.7$, consistent with the benchmark trend that dynamic heterogeneity grows on
cooling. The companion spatial-front row inverts the same $\xi_4(T)$ values
back to $D_f(T)$ and requires the relative standard deviation to remain below
$5\%$; the generated scan gives a near-zero numerical spread because it was
constructed from one front diffusivity. A stretched-alpha row fits the same
facilitated finite-exchange scan to a KWW window: cooling lowers
$\beta_{\rm KWW}$ from $0.913$ to $0.715$ with cold-end residual $0.064$,
consistent with stretched alpha relaxation while remaining a diagnostic fit,
not a unique microscopic barrier-distribution derivation. A joint
persistence/exchange row then requires one inverted clock pair to pass the
multi-$k$ alpha check, late-NGP recovery, Stokes-Einstein growth, and the
$\chi_4$ proxy simultaneously: the consistent case gives
$\tau_p/\tau_x=8.00$, $D\tau_\alpha$ growth $3.56$, zero multi-$k$ and
late-NGP residuals, and $\chi_4$-proxy growth $2.58$, while the deliberately
corrupted multi-$k$ case is rejected with residual $0.223$. These are still
not fits to raw trajectory data; they are
reproducible bridges from the SOTA comparison table to checkable
model-versus-benchmark rows.
The source-level alignment file
\texttt{renewal\_cage\_sota\_claim\_alignment.csv} adds a stricter guard against
overclaiming. It maps each cited benchmark claim to one of four statuses:
derived dynamical support, partial effective closure, explicit thermodynamic
scope boundary, or not supported. Under this audit, Kob-Andersen caging/NGP and
self-intermediate-scattering claims, Hedges persistence/exchange decoupling,
and Fickian non-Gaussian diffusion are marked as derived dynamical diagnostics;
four-point dynamic length and experimental dynamic-heterogeneity claims are
marked as effective spatial closures; Angell fragility is a barrier-law proxy;
and Kauzmann/Adam-Gibbs thermodynamics is an explicit closure boundary rather
than a consequence of renewal dynamics alone.
The real-benchmark assimilation gate in
\texttt{renewal\_cage\_real\_benchmark\_assimilation\_gate.csv} then asks a
more operational pre-fit question: does a candidate source have the required
observable set, a shared system and temperature grid, machine-readable curves,
and uncertainty estimates? Under this gate, the Kob-Andersen alpha/van-Hove
fusion is promoted only to a structural digitization candidate until
uncertainty columns are supplied; Hedges persistence/exchange curves are
blocked from the joint $\chi_4$/late-NGP protocol by missing held-out
observables; spatial heterogeneity benchmarks remain effective-closure inputs;
and the thermodynamic entropy row is kept as a scope boundary rather than a
renewal-dynamics inversion.
The frontier horizon file
\texttt{renewal\_cage\_frontier\_benchmark\_horizon.csv} updates this audit
with recent SOTA directions: the GlassBench/machine-learning roadmap
\cite{jung2025roadmap} is classified as a trajectory reanalysis candidate
because missing $F_s$, NGP, and diffusion observables can in principle be
computed from particle trajectories; the Ge--Sb--Te neural-network-potential
study of viscosity, Stokes--Einstein breakdown, and heterogeneity
\cite{marcorini2025gst} is a transport/heterogeneity candidate but still lacks
late-NGP and multi-$k$ alpha inputs for the full renewal inversion; molecular
motion near the experimental glass transition \cite{simon2026molecular} is
flagged as requiring a translation--rotation extension of the present scalar
renewal clock; and heat-capacity or entropy benchmarks remain thermodynamic
scope boundaries.
The separate file
\texttt{renewal\_cage\_literature\_inversion\_readiness.csv} audits the
data-readiness of representative benchmark papers. It marks the
Kob-Andersen van-Hove and intermediate-scattering papers
\cite{kob1995vanhove,kob1995intermediate}, Hedges persistence/exchange
\cite{hedges2007persistence}, Weeks-Weitz cage rearrangements
\cite{weeks2002cage}, four-point data \cite{lacevic2003fourpoint}, and
Fickian non-Gaussian colloid data \cite{guan2014fickian} as qualitatively
useful but not yet machine-readable, uncertainty-weighted inversion inputs in
this repository. This is the current boundary between literature-level SOTA
agreement and a future raw-data fit.
The file
\texttt{renewal\_cage\_observable\_falsification\_matrix.csv} refines this
boundary by asking a stricter question: for each benchmark paper, which
diagnostic protocols can be falsified from the observables actually reported,
and which missing observable is the primary blocker?
The companion
\texttt{renewal\_cage\_benchmark\_fusion\_readiness.csv} adds the next
constraint for multi-paper validation: observables may be fused only when
system identity, temperature grid, and simulation or experimental ensemble are
shared. Under this rule, Kob-Andersen parts I and II form a structural
van-Hove/alpha benchmark, but splicing Hedges persistence clocks or
La{\v{c}}evi{\'c} four-point data into the same quantitative diagnostic remains
blocked unless the raw observables are remeasured on a common ensemble.
Finally,
\texttt{renewal\_cage\_raw\_curve\_ingestion\_contract.csv} specifies the
column-level contract for turning the structurally valid Kob-Andersen I/II
fusion into an actual uncertainty-weighted inversion: self-intermediate
scattering requires \texttt{temperature}, \texttt{wave\_number}, \texttt{time},
and \texttt{F\_s} plus \texttt{sigma\_F\_s}; the van-Hove/NGP input requires
\texttt{G\_s}, $\alpha_2$, diffusion, and their uncertainties. Thus the paper
does not treat digitized curves as a full real-data fit until the uncertainty
columns are supplied.
The file
\texttt{renewal\_cage\_raw\_curve\_diagnostic\_readiness.csv} then aggregates
these observable-level contracts to the diagnostic level, so that the multi-$k$
alpha-shape, van-Hove recovery, and combined alpha/van-Hove/transport closure
each carry separate structural-readiness and uncertainty-readiness flags.
The companion
\texttt{renewal\_cage\_raw\_curve\_persistence\_exchange\_protocol.csv}
demonstrates the full synthetic-to-diagnostic path: machine-readable
$F_s(k,t)$ curves are reduced to $\tau_\alpha(k)$ by threshold crossing, the
late NGP and $\chi_4$ proxy are extracted directly from raw time series, and the
same persistence/exchange clocks must then pass the multi-$k$, late-NGP,
Stokes-Einstein, and $\chi_4$ tests. A deliberately corrupted late-NGP curve is
rejected without refitting the clocks.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_results.pdf}
  \caption{MSD, NGP, and parameter sweeps for the delayed renewal cage model.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_dimensionless.pdf}
  \caption{Dimensionless NGP collapse and radial van Hove distributions compared
  with matched Gaussian baselines.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_scattering.pdf}
  \caption{Closed-form self-intermediate scattering predictions. The left panel
  shows a cage plateau followed by alpha relaxation; the right panel removes
  the local cage Debye-Waller factor and isolates the renewal-controlled decay.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_temperature.pdf}
  \caption{Temperature-dependent renewal diagnostics. Cooling decreases the
  long-time diffusion coefficient while increasing the alpha-relaxation and NGP
  peak times. The normalized product $D\tau_\alpha$ grows when the delayed
  renewal control parameter $\lambda\tau_d$ grows, and the same scan gives a
  fractional Stokes-Einstein exponent $\xi_{\rm SE}<1$ plus a growing apparent
  alpha-relaxation activation energy.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_alpha_shape.pdf}
  \caption{Alpha-relaxation time-temperature-superposition diagnostic. The left
  panel plots the cage-normalized alpha shape after scaling time by
  $\tau_\alpha$. The right panel shows the collapse residual together with the
  control variable $C_k=\Gamma_k\lambda\tau_d$, demonstrating that the delayed
  renewal clock predicts a specific, measurable source of alpha-shape
  superposition breakdown.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_facilitated_exchange.pdf}
  \caption{Facilitated finite-exchange heterogeneity scan. Cooling both broadens
  the instantaneous mobility distribution and slows exchange between mobility
  environments, producing a growing exchange ratio $c=R_x/\kappa_0$, enhanced
  late NGP amplitude, and a reduced alpha decay per renewal.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_persistence_exchange.pdf}
  \caption{Persistence-exchange renewal diagnostic. The first cage escape time
  and subsequent exchange time are separated. Increasing $\tau_p/\tau_x$ at
  fixed exchange time leaves the long-time diffusion coefficient fixed but
  increases $D\tau_\alpha$ and still permits long-time NGP recovery.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_persistence_exchange_protocol.pdf}
  \caption{Persistence-exchange inversion protocol. Long-time diffusion and the
  cage-normalized alpha time infer the hidden exchange and first-persistence
  clocks. The late NGP is not fitted; it is a held-out falsification observable
  that accepts the consistent synthetic case and rejects a mismatched late-NGP
  case.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_persistence_exchange_joint_protocol.pdf}
  \caption{Joint persistence-exchange inversion protocol. An anchor alpha time
  and long-time diffusion infer the hidden clocks. The multi-$k$ alpha times,
  late NGP, and $\chi_4$ proxy are then held-out checks; the mismatched
  multi-$k$ case fails without refitting the clocks.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_persistence_exchange_uncertainty_protocol.pdf}
  \caption{Uncertainty-weighted persistence-exchange data protocol. Log
  residuals for multi-$k$ alpha times, late NGP, and the $\chi_4$ peak are
  divided by measurement-error estimates to give directly falsifiable
  $z$-scores.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_glass_audit.pdf}
  \caption{Glass-dynamics phenomenon audit. One delayed-renewal finite-exchange
  law is tested against common dynamical glass signatures. The audit supports
  the dynamical signatures listed in the text and explicitly leaves the
  thermodynamic-transition flag unsupported.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_glass_phase_diagram.pdf}
  \caption{Barrier-facilitation signature phase diagram. The scan separates
  delay-barrier growth from exchange-barrier growth and records when the
  dynamical closure criteria are jointly satisfied.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_spatial_chi4.pdf}
  \caption{Spatial facilitation closure for the four-point amplitude. A
  diffusive facilitation front converts the persistence clock into a dynamic
  length $\xi_4$ and correlated renewal volume $N_{\rm corr}$, making the
  renewal contribution to the $\chi_4$ peak grow on cooling without fitting an
  external domain size.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_thermodynamic_closure.pdf}
  \caption{Thermodynamic entropy closure. A Kauzmann-style configurational
  entropy extrapolation and excess heat capacity are coupled to the renewal
  persistence clock through an Adam-Gibbs relaxation time, producing
  super-Arrhenius alpha slowdown while keeping the entropy law explicit.}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_mct_beta_closure.pdf}
  \caption{Effective MCT beta-window closure. The added envelope supplies the
  critical approach and von Schweidler departure around the cage plateau,
  together with a clock-separation diagnostic between beta and alpha
  relaxation.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_sota_benchmark_consistency.pdf}
  \caption{SOTA benchmark consistency diagnostic. The first row checks
  Debye-Waller cage localization, renewal leakage during the plateau, and
  alpha-cage time-scale separation. The second row encodes the
  Kob-Andersen beta-window conclusion that late von-Schweidler behavior is
  visible while early critical decay is not cleanly resolved. The third row
  checks whether the fitted $a$ and $b$ exponents share a common MCT exponent
  parameter $\lambda$ within a $5\%$ internal-consistency threshold. The fourth row
  checks the long-time Gaussian-recovery mechanism selection against a static
  mobility-disorder null. The fifth row checks the transient NGP peak shift on
  cooling together with late Gaussian recovery. The sixth row checks fractional
  Stokes-Einstein decoupling through $D\tau_\alpha$ growth and
  $\xi_{\rm SE}<1$. The seventh row checks dynamic-heterogeneity growth through
  $\xi_4$, $N_{\rm corr}$, and the $\chi_4$ peak. The eighth row checks whether
  the same $\xi_4(T)$ values invert to a temperature-independent
  facilitation-front diffusivity. The ninth row
  checks alpha-shape TTS breakdown through the
  cold-end scaled-shape residual and the growth of
  $C_k=\Gamma_k\lambda\tau_d$. The tenth row checks KWW stretched-alpha
  relaxation through the fitted $\beta_{\rm KWW}$ cooling trend and fit
  residual. The eleventh row encodes the
  persistence/exchange protocol: diffusion fixes $\tau_x$, $\tau_\alpha$
  inverts $\tau_p$, and late NGP remains a held-out falsification observable.
  The twelfth row requires the same persistence/exchange clocks to pass
  multi-$k$ alpha, late-NGP, Stokes-Einstein, and $\chi_4$-proxy checks while
  rejecting a deliberately corrupted multi-$k$ alpha input. The thirteenth row
  checks that the self van Hove distribution has a transient
  peak-time tail excess while its late-time tail returns to a matched Gaussian.
  The fourteenth row checks Angell/Adam-Gibbs fragility growth
  \cite{angell1995formation,adam1965temperature} through apparent activation
  and local fragility growth while preserving the scope boundary that
  material-specific fragility origins are not derived. The fifteenth row records
  the thermodynamic-transition scope boundary: heat-capacity and Kauzmann
  signatures require the explicit entropy closure and are not derived from
  renewal dynamics alone.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_sota_claim_alignment.pdf}
  \caption{Source-level SOTA claim alignment. Each cited benchmark claim is
  mapped to a model diagnostic, support level, data-readiness state, and primary
  blocker. Dynamical signatures are separated from effective spatial closures
  and from thermodynamic-transition scope boundaries.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_real_benchmark_assimilation_gate.pdf}
  \caption{Real-benchmark assimilation gate. Representative SOTA sources are
  checked for protocol-required observables, shared-system consistency,
  machine-readable curves, uncertainty estimates, and scope boundaries before
  any claim of quantitative real-data inversion.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_literature_inversion_readiness.pdf}
  \caption{Literature inversion readiness audit. The bars measure the fraction
  of model-required observables available from representative benchmark
  papers, while the flags separate qualitative comparison readiness from
  quantitative and uncertainty-weighted inversion readiness.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_observable_falsification_matrix.pdf}
  \caption{Observable-to-diagnostic falsification matrix. Each benchmark paper
  is crossed with the van-Hove recovery, multi-$k$ alpha-shape,
  persistence/exchange-$\chi_4$, Stokes-Einstein, and four-point facilitation
  diagnostics. Blue cells have the structural observables needed for a
  falsification check but still lack machine-readable uncertainty estimates;
  yellow and red cells identify the first missing observable that blocks the
  corresponding diagnostic.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_benchmark_fusion_readiness.pdf}
  \caption{Cross-benchmark fusion readiness. The first row shows that the two
  Kob-Andersen papers have the shared system, temperature grid, and ensemble
  needed for a structural van-Hove/alpha closure check, while the absence of
  machine-readable curves and uncertainty estimates blocks quantitative
  inversion. The remaining rows mark cross-paper splices that contain useful
  observables but fail the shared-grid or shared-ensemble requirement.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_raw_curve_ingestion_contract.pdf}
  \caption{Raw-curve ingestion contract for the structurally valid
  Kob-Andersen I/II fused benchmark. The required observable columns are
  present for the synthetic machine-readable contract, but the
  uncertainty-weighted inversion remains blocked until the listed uncertainty
  columns are available.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_raw_curve_diagnostic_readiness.pdf}
  \caption{Diagnostic-level readiness implied by the raw-curve ingestion
  contract. The KA I/II fused benchmark has the structural columns needed for
  the listed diagnostic protocols, but each protocol remains below the
  uncertainty-weighted real-inversion threshold until the corresponding
  uncertainty columns are supplied.}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/renewal_cage_raw_curve_persistence_exchange_protocol.pdf}
  \caption{Raw-curve persistence/exchange inversion protocol. Synthetic
  machine-readable $F_s(k,t)$, late-NGP, diffusion, and $\chi_4$ curves are
  reduced to observable clocks and scored with the uncertainty-weighted
  persistence/exchange protocol. The consistent raw curves pass all held-out
  checks, while a late-NGP mismatch fails without refitting.}
\end{figure}

\clearpage
\section{Discussion}

The model is not intended to replace molecular simulations or microscopic cage
jump analysis. Its role is to isolate a minimal mechanism: delayed renewal of
local cages creates a finite-time NGP peak while preserving ordinary long-time
Gaussian recovery. The delay time $\tau_d$ mainly controls when the peak
appears; the ratio $q/A$ controls its height in the plateau regime. This
separation of roles may be useful when interpreting short-time simulations or
coarse-grained cage-jump statistics near glass transition.
The generalized exponent argument also makes a falsifiable modeling
assumption explicit: a measured early-time NGP scaling
$\alpha_2(t)\propto t^\beta$ corresponds to an effective renewal-delay
exponent $m=\beta+1$ within this class. The square-delay model therefore
predicts an initially linear NGP rise, not merely a finite peak. The
self-intermediate scattering result adds an independent glass-literature
observable: $\tau_\alpha(k)$ is controlled by the same renewal rate that appears
in the NGP consistency check. The temperature extension adds a separate
criterion for Stokes-Einstein violation: changing $\lambda(T)$ alone mainly
rescales both diffusion and relaxation, while growth of $\tau_d(T)$ relative to
$1/\lambda(T)$ produces decoupling through the inverse renewal shape $F^{-1}$.
The activated-barrier interpretation makes this criterion sharper:
$E_d>E_\lambda$ is the condition for $\lambda(T)\tau_d(T)$ to grow on cooling.

Several assumptions are deliberately restrictive. Renewal events are taken to
be Poisson once the delayed intensity is specified; cage-center jumps are
Gaussian and uncorrelated; and the local cage variance is reduced to a single
relaxation scale. These choices are what make the moment hierarchy and van Hove
mixture analytic. Their role is diagnostic rather than microscopic: correlated
jumps, broad waiting-time distributions, or non-Gaussian local cage motion would
modify the peak shape, but they would have to reproduce the same four
observables--the MSD plateau, the early-time NGP exponent, the peak/late-time
renewal-rate consistency, and the long-time $1/t$ decay--to fall in the same
renewal-cage universality class.

Table~\ref{tab:comparison} summarizes the distinction from nearby modeling
languages. The point is not that a renewal model is the only explanation of a
non-Gaussian peak. Rather, the delayed renewal construction supplies an
explicit bridge between a cage plateau, a finite-time NGP maximum, a radial van
Hove tail, the early-time exponent, and peak/late-time diagnostics that can be
checked against simulation trajectories.

\begin{center}
\refstepcounter{table}\label{tab:comparison}
\textbf{TABLE~\thetable. Relation to nearby mechanisms for non-Gaussian
transport.}

\begin{tabular}{llll}
Model class & Fluctuator & NGP mechanism & Diagnostic here \\
\hline
Harmonic cage & Displacement & None & MSD plateau \\
Diffusing diffusivity & Mobility & Heterogeneity & Late $1/t$ recovery \\
Cage-jump picture & Events & Intermittency & Delayed $R(t)$ \\
Delayed renewal cage & Renewal count & $qR(t^\ast)\simeq A$ & $\lambda_\ell\simeq\lambda_\ast$ \\
\end{tabular}
\end{center}

\bibliographystyle{apsrev4-2}
\bibliography{references}

\end{document}
